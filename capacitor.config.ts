import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.rhydlewis.apps.adventurerrpg',
  appName: 'adventurer-rpg',
  webDir: 'dist'
};

export default config;
